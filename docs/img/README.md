Screenshots referenced by the root README. All DARK theme, real Delhi/Chennai
data — not the synthetic world. Keep the theme consistent when retaking.

  console-map.png         admin console — a hotspot zone selected, with its
                          attributed source, evidence chain, EPS breakdown and a
                          routed inspection drawn on the map
  chennai-zones.png       the same console switched to Chennai — twelve
                          enforcement zones, proving the pipeline is not
                          Delhi-specific
  citizen-find-ward.png   citizen entry — citywide AQI, search by name, use my
                          location, or tap the map
  citizen-ward.png        a ward view — AQI and "why your air is like this" on a
                          ward where no source was attributed
  citizen-forecast.png    the same ward scrolled — nearest-monitor distance, the
                          advisory, and the 3-hourly forecast with the cleanest
                          and worst hours named
  citizen-report.png      the report form — category, optional photo, optional
                          description
  ledger.png              the intervention ledger — response time measured,
                          effectiveness explicitly not measured
  telegram-voice-loop.png the citizen channel both ways — a spoken Hinglish
                          report transcribed and logged, answered with a Hindi
                          audio advisory

If the UI changes materially, retake them: a README showing a version of the
product that no longer exists is the same bug as a stale metric.
